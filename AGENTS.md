# PicEmu 项目 Agent 规则

本文件是 PicEmu 仓库的项目级开发约定。所有在本目录及子目录工作的 Agent
都必须遵守。若用户在当前任务中给出更具体的要求，以用户最新要求为准。

## 交流与语言

- 与用户交流使用中文，先说明结果，再补充必要的技术细节。
- 以后所有新增或修改的代码都应尽量添加详细、准确的中文注释，尤其要说明
  文件用途、硬件连接、寄存器/位掩码含义、关键算法、时序要求、边界条件和
  设计取舍；注释应帮助新手理解“为什么这样实现”，不能只把代码逐句翻译成
  中文，也不能为了增加注释数量而重复显而易见的信息。
- Git 提交说明必须使用中文。
- 进展更新保持简洁；持续执行工具任务时，不要让用户长时间看不到进度。
- 不确定之处优先通过现有代码、文档、数据手册和实际测试确认，不凭空猜测。

## 总体目标

PicEmu 是一个供新手学习、固件验证和跨平台移植使用的 PIC10 系列模拟器。
实现应保持清晰、模块化和可扩展，不能只让某一个示例“看起来能运行”。

当前重点型号是 PIC10F200 和 PIC10F202。后续可能增加 PIC10F204、PIC10F206
以及其他芯片，因此任何设计都不应把型号、外设或固件逻辑散落写死在前端和
平台入口中。

## 架构原则

- CPU 核心、固件加载、仿真器件、电路连接、平台适配和前端显示必须分层。
- 公共核心使用 C11，能够在 Linux 下通过 GCC 编译。
- 模块按功能放入清晰的子目录；不要把所有逻辑堆在 `src/`、`include/`
  或某个前端主文件中。
- 每种虚拟器件拥有独立的头文件和实现文件。
- 新增器件通过注册或统一接口接入，避免每增加一个器件就修改包含所有具体
  器件函数的中央头文件。
- SDL、Web、STM32 和 FPGA 不得各自复制一套 PIC 指令语义。
- 不允许针对某个示例固件在 CPU、FPGA 或平台层加入特殊处理。遇到示例不能
  运行时，应修复缺失指令、外设模型或通用接口。
- 不人为限制 MCU 数量；若底层确有资源限制，应明确记录原因，而不是使用
  随意常量。

## 芯片与固件

- Intel HEX 必须由共享加载器解析并执行；配置字与程序存储器地址应正确区分。
- PIC10F200 和 PIC10F202 使用同一 Baseline 12 位核心，但程序空间和 RAM
  容量由器件描述决定。
- 尊重 PIC10 的真实硬件限制，例如两级硬件栈、GP3 仅输入、没有中断、UART、
  硬件 SPI 和硬件 PWM。
- 软件模拟 SPI、UART、PWM 等功能时，应由 PIC 固件正常翻转 GPIO 实现。
- 指令周期和外设时间必须基于 PIC 时钟计算。宿主机频率、日志开关或前端帧率
  不应改变虚拟 PIC 时间。
- 优化性能后必须验证指令语义、周期数和实际时间没有发生错误。

## 虚拟器件与电路文件

- 电路由 `diagram.json` 描述，不把示例电路写死在 SDL 或 Web 主程序中。
- SDL 与 Web 尽量共用同一份 `diagram.json` 和相同的世界坐标、器件尺寸、
  属性及连接关系。
- 器件容量、颜色、固件路径等实例属性放在器件的 `attrs` 中。
- 连线端点必须真实对应器件引脚；线路不得从器件图形下面穿过造成接错引脚的
  视觉错觉。
- 自定义布线路径只使用水平和垂直线段，拐点自动生成直角，不显示斜线。
- 外设模型实现协议和状态，不在显示层伪造结果。
- W25Q 等存储器的查看器应读取真实 C 器件模型，数据变化后实时更新。

## 示例组织

- 所有示例放在 `examples/<示例名>/`，每个示例独立包含：
  - `main.c`
  - `Makefile`
  - `diagram.json`
  - 自动生成的 `build/`
- 示例固件和电路文件不得散落在仓库根目录。
- 示例代码应展示通用接口。例如存储器示例应提供
  `w25q_write(address, data, length)`、`w25q_read(...)` 一类接口，
  `main()` 只组织业务流程，不把底层函数写死为只能处理某个字符串。
- 根据程序空间和 RAM 需求选择合适型号。PIC10F200 资源不足时可使用
  PIC10F202，但必须在 Makefile、HEX 编译参数和电路图中保持型号一致。
- 编译 XC8 固件时检查程序空间、RAM 和硬件栈警告；不能只看是否生成 HEX。

## 构建目录与清理

- 所有中间文件和最终产物必须进入专用 `build/`，不能混入源码目录。
- 根目录 `make clean` 应清理受管理的命令行、SDL、Web、STM32 和 FPGA
  构建产物；不能遗留测试生成的空目录或临时文件。
- 平台专用工具放在对应平台目录。例如只供 STM32 使用的工具应位于
  `ports/stm32f103/tools/`。
- Linux/WSL 的 Makefile 或 Shell 脚本是统一构建逻辑的主入口。
- PowerShell 脚本只负责参数转换和调用统一入口，不复制维护另一套构建规则。
- 新增 Makefile 参数时，PowerShell 包装层应尽量透明传递，避免两边同时修改。

## Web 前端

- Web 使用本地 Node 服务调用共享 C 后端，不在 JavaScript 中重写 PIC CPU。
- 支持运行、暂停、精确单步、复位、断点、程序跟随 PC，以及寄存器、栈、RAM、
  程序 Flash 和外设数据查看。
- 每颗 MCU 可设置独立 HEX；多颗 MCU 的状态、断点和程序面板按实例 ID 隔离。
- 画布支持鼠标中心缩放、中键四向平移、框选、多选移动和批量删除。
- 器件移动时坐标实时更新；操作按键不应强制切换用户正在观察的程序页签。
- 器件属性属于具体实例，不把 HEX、容量等属性放成页面全局设置。
- 数据查看和高频状态更新应节流，避免闪烁、卡顿和无意义重绘。
- 修改 UI 后必须实际启动 Web，并用浏览器检查布局、交互和数据，而不只做
  JavaScript 语法检查。

## SDL 前端

- SDL 器件渲染和仿真器件模型分离，不能把所有器件逻辑写在 SDL 主程序中。
- 窗口支持缩放和调整大小，渲染循环保持稳定，不通过持续修改窗口标题输出日志。
- GPIO、波形和器件状态来自共享仿真状态。
- SDL 和 Web 的网格、器件尺寸和连接位置应尽量一致。

## STM32F103 端口

- 端口目录统一使用 `ports/stm32f103/`，不要按 C4/C6/C8/CB/CC 重复创建目录。
- 默认引脚映射可使用虚拟 GP0 → PC13，但映射必须集中配置，不能散落写死。
- 所选示例和固件通过构建参数指定，不能固定为 `blink`。
- 日志默认关闭，通过构建参数选择开启；关闭日志不应改变模拟时间。
- 系统时钟通过统一构建参数配置，默认可为 128 MHz，并允许受支持的其他频率。
- 时间同步使用实际系统时钟参数计算，不能靠为某个频率写死校准值。
- 提供命令行编译和 ST-Link 烧录流程，避免要求用户在 WSL 和 Windows GUI
  之间反复切换。

## Tang Nano 1K FPGA

- FPGA 代码放在 `fpga/`，按 RTL、约束、脚本、工具和测试平台划分。
- 使用 Windows 下的 OSS-CAD-Suite 命令行构建和烧录，不依赖官方 IDE。
- FPGA 实现独立 Verilog PIC10F200 核心，不能调用软件 C 模拟器。
- PIC 程序 ROM 随位流写入 FPGA 配置 Flash，运行时使用片上 BSRAM 取指，
  不浪费 LUT 表示程序存储器。
- 更换当前方案中的 HEX 需要重新生成并烧录位流，文档必须明确 CPU 核心、
  ROM 初始化文件、配置 Flash 和运行时 BSRAM 的关系。
- 测试平台只验证通用 CPU 行为，不保留针对流水灯等示例的特殊 RTL。
- 修改 RTL 后应命令行构建；用户要求实板验证时，应自动烧录并依据用户观察
  继续定位，不能只声称综合通过。

## 测试要求

每次功能修改应按风险选择并实际执行相关测试：

1. C 编译器警告检查；
2. CPU 和虚拟器件单元测试；
3. 示例固件 XC8 编译；
4. 命令行和 Web 后端集成测试；
5. SDL 器件测试；
6. Web 实际浏览器交互测试；
7. STM32 或 FPGA 修改对应的交叉编译、烧录和实板验证。

完成前至少执行 `git diff --check`。不要声称通过未执行的测试。若受硬件或工具
限制无法验证，应明确说明未验证的部分。

## Git 与工作区安全

- 用户明确要求“提交”或“推送”时才执行相应 Git 操作。
- 提交前确认测试通过、提交范围正确、提交说明为中文。
- 工作区中已有的修改默认属于用户或其他任务。不得覆盖、删除、格式化或提交
  与当前任务无关的改动。
- 禁止使用 `git reset --hard`、`git checkout --` 等破坏性命令清理用户修改。
- 删除文件或测试目录前先确认目标属于当前任务且可以恢复。

## 文档

- 主 `README.md` 只介绍项目能力、快速开始、模块入口和文档导航。
- 详细说明按主题放入 `docs/`，不要把所有内容堆回主 README。
- 新增芯片、器件、示例、平台参数或限制时同步更新对应文档。
- 文档中的命令必须与当前 Makefile 和脚本一致，并优先给出可直接复制执行的
  Linux/WSL 与 PowerShell 示例。
