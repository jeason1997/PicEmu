# PicEmu 项目 Agent 规则

本文件定义整个 PicEmu 仓库共同遵守的项目级约定。子目录中的 `AGENTS.md`
会在本文件基础上补充该领域的具体要求；若用户在当前任务中给出更具体的要求，
以用户最新要求为准。

## 规则索引

- `src/AGENTS.md`：公共 C 核心、芯片、固件加载、虚拟器件与电路模型。
- `examples/AGENTS.md`：示例目录、XC8 固件和电路文件。
- `frontends/AGENTS.md`：SDL 与 Web 共享的电路布局和显示边界。
- `frontends/web/AGENTS.md`：Web 前后端、交互和浏览器验证。
- `frontends/sdl/AGENTS.md`：SDL 渲染、交互和共享仿真状态。
- `ports/stm32f103/AGENTS.md`：STM32F103 构建、时钟、烧录和实板验证。
- `fpga/AGENTS.md`：Tang Nano 1K 的 RTL、工具链、位流和实板验证。
- `docs/AGENTS.md`：README 和主题文档的组织方式。

`include/picemu/` 是 `src/` 公共实现的接口层。修改公开接口时，必须同时遵守
`src/AGENTS.md`；修改根目录 `README.md` 时必须同时遵守 `docs/AGENTS.md`；修改
测试时，应同时参照被测模块对应的局部规则。

## 交流与语言

- 与用户交流使用中文，先说明结果，再补充必要的技术细节。
- 新增或修改的代码应尽量添加详细、准确的中文注释，尤其说明文件用途、硬件
  连接、寄存器或位掩码含义、关键算法、时序要求、边界条件和设计取舍。
- 注释应帮助新手理解“为什么这样实现”，不能只逐句翻译代码，也不要重复显而
  易见的信息。
- Git 提交说明必须使用中文。
- 进展更新保持简洁；持续执行工具任务时，不要让用户长时间看不到进度。
- 不确定之处优先通过现有代码、文档、数据手册和实际测试确认，不凭空猜测。

## 总体目标与架构红线

PicEmu 是供新手学习、固件验证和跨平台移植使用的 PIC10 系列模拟器。实现应
保持清晰、模块化和可扩展，不能只让某一个示例“看起来能运行”。当前重点型号
是 PIC10F200 和 PIC10F202，后续还可能增加其他芯片，因此不得把型号、外设或
固件逻辑散落写死在前端和平台入口中。

- CPU 核心、固件加载、仿真器件、电路连接、平台适配和前端显示必须分层。
- 公共核心使用 C11，并能够在 Linux 下通过 GCC 编译。
- 模块按功能放入清晰的子目录，不把所有逻辑堆在 `src/`、`include/` 或某个
  前端主文件中。
- SDL、Web 和 STM32 必须共享同一套 C 核心指令语义；FPGA 使用独立 RTL 核心，
  但不得为示例加入特殊指令行为。
- 不允许针对某个示例固件在 CPU、FPGA 或平台层加入特殊处理。示例不能运行时，
  应修复缺失指令、外设模型或通用接口。
- 不人为限制 MCU 数量；底层确有资源限制时，应明确记录原因，不使用随意常量。

## 构建目录与工具链

- 所有中间文件和最终产物必须进入专用 `build/`，不能混入源码目录。
- 根目录 `make clean` 应清理受管理的命令行、SDL、Web、STM32 和 FPGA 构建
  产物，不得遗留测试生成的空目录或临时文件。
- 平台专用工具放在对应平台目录。
- Linux/WSL 的 Makefile 或 Shell 脚本是统一构建逻辑的主入口。
- PowerShell 脚本只负责参数转换和调用统一入口，不复制维护另一套构建规则；
  新增 Makefile 参数时应尽量透明传递。
- 编译器、DFP 和其他工具路径优先从环境变量及 `PATH` 读取，不在 Makefile、
  脚本或 Agent 命令中写死用户或版本路径。WSL 工具配置在 `.bashrc` 时，应使用
  交互 shell 加载；确实没有配置时，再设置环境变量并记录方法。

## 测试要求

每次功能修改应按影响范围和风险实际执行相关测试，包括但不限于：

1. C 编译器警告检查；
2. CPU 和虚拟器件单元测试；
3. 示例固件 XC8 编译；
4. 命令行和 Web 后端集成测试；
5. SDL 器件测试；
6. Web 实际浏览器交互测试；
7. STM32 或 FPGA 修改对应的交叉编译、烧录和实板验证。

完成前至少执行 `git diff --check`。不得声称通过未执行的测试；受硬件或工具限制
无法验证时，应明确说明未验证的部分。

## Git 与工作区安全

- 只有用户明确要求“提交”或“推送”时，才执行相应 Git 操作。
- 每次提交前必须先执行根目录统一清理（通常为 `make clean`），确保构建产物不
  混入提交；删除本任务临时创建且不属于正式测试套件的测试代码、数据和辅助
  文件，并清理因此留下的空目录。
- 不得把临时文件清理规则误用于仓库长期维护的 `tests/` 测试源码。
- 提交前确认测试通过、提交范围正确，并使用中文提交说明。
- 工作区已有修改默认属于用户或其他任务，不得覆盖、删除、格式化或提交无关改动。
- 禁止使用 `git reset --hard`、`git checkout --` 等破坏性命令清理用户修改。
- 删除文件或测试目录前，先确认目标属于当前任务且可以恢复。
